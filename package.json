{
  "name": "language-benchmarks",
  "version": "0.0.1",
  "description": "A node wrapper around language benchmarks",
  "main": "start.js",
  "scripts": {
    "clean": "rm -rf ./*.out && rm -rf ./src/**/*.out && rm -rf ./src/csharp/**/bin ./src/csharp/**/obj",
    "prestart": "npm run clean",
    "start": "node ./src/main.js"
  },
  "dependencies": {
    "pidusage": "^2.0.6"
  }
}