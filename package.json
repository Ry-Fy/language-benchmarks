{
  "name": "language-benchmarks",
  "version": "0.0.1",
  "description": "A node wrapper around language benchmarks",
  "main": "start.js",
  "scripts": {
    "clean": "rm -rf -d ./src/**/*.out",
    "prestart": "npm run clean",
    "start": "node ./src/main.js"
  },
  "dependencies": {
    "pidusage": "^2.0.6"
  }
}